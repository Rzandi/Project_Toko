# InvoiceEase — Backend

Minimal Express + TypeScript scaffold. To run locally:

```powershell
cd backend
npm install
copy .env.example .env
# edit .env if needed
npm run dev
```

This scaffold includes models in `src/models`, basic routes in `src/routes` and simple controllers in `src/controllers`.
